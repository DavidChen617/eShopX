@startuml

note as TODO

  == TODO ==

  [ ] 統一 Domain URL 設定
      目前 LinePay confirmUrl / cancelUrl
      和 PayPal returnUrl / cancelUrl
      各自寫死 public URL，需改為統一從設定檔讀取。

      影響範圍：
      - LinePayService（confirmUrl, cancelUrl）
      - PayPalService（returnUrl, cancelUrl）
      - ECPayService（clientReplyUrl, serverReplyUrl）

      做法：
      - appsettings.json 新增 AppSettings:DomainUrl
      - 各 Service 注入 IOptions<AppSettings> 取得 DomainUrl
      - Callback path 各自定義，DomainUrl 只維護一個

end note

@enduml
