@startuml

note as N1
    OTP 不存 DB，改用 Redis 暫存

    Key:   otp:{email}
    Value: OTP 碼（6 位數字）
    TTL:   5 分鐘（到期 Redis 自動清除）

    流程：
    - 發送 OTP → SET otp:{email} {otp} EX 300
    - 重新發送  → 覆蓋同一個 key（舊的自動失效）
    - 驗證 OTP → GET otp:{email} 比對，成功後 DEL
    - OTP 過期  → GET 回 null → 回 400
end note

@enduml
